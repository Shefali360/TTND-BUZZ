import React from "react";
import ComplaintsList from "./AllComplaintsList/AllComplaintsList";

const ResolvedPage = (props) => {
  return (
    <div>
      <ComplaintsList />
    </div>
  );
};

export default ResolvedPage;
